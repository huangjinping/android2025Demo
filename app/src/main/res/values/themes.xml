<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Theme.Android2023Demo" parent="Theme.MaterialComponents.DayNight.DarkActionBar">
        <!-- Primary brand color. -->
        <item name="colorPrimary">@color/purple_500</item>
        <item name="colorPrimaryVariant">@color/purple_700</item>
        <item name="colorOnPrimary">@color/white</item>
        <!-- Secondary brand color. -->
        <item name="colorSecondary">@color/teal_200</item>
        <item name="colorSecondaryVariant">@color/teal_700</item>
        <item name="colorOnSecondary">@color/black</item>
        <item name="android:forceDarkAllowed" tools:targetApi="q">false</item>

        <!-- Status bar color. -->
        <item name="android:statusBarColor" tools:targetApi="l">?attr/colorPrimaryVariant</item>
        <!-- Customize your theme here. -->
    </style>

    <!--  采用google自带的 SplashScreen  -->
    <style name="SplashTheme" parent="Theme.SplashScreen">
        <!--  启动画面背景颜色  -->
        <item name="windowSplashScreenBackground">@color/cardview_dark_background</item>
        <!--  启动画面icon图标：这里可以是图片、帧动画等  -->
        <item name="windowSplashScreenAnimatedIcon">@mipmap/ceshi</item>
        <item name="splashScreenIconSize">10sp</item>
        <item name="windowSplashScreenIconBackgroundColor">@color/cardview_dark_background</item>
        <!--  icon动画在关闭之前显示的时长：最长时间为1000毫秒  -->
        <item name="windowSplashScreenAnimationDuration">300</item>
        <!--  Splash退出后的主题  -->
        <item name="postSplashScreenTheme">@style/Theme.Android2023Demo</item>

    </style>

    <style name="BaseTheme" parent="Theme.MaterialComponents.Light.NoActionBar">
        <!--  应用的主要品牌颜色，主要用于主题  -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <!--  主要品牌颜色的较浅/较暗变体，在主题中很少使用  -->
        <item name="colorPrimaryVariant">@color/colorPrimaryDark</item>
        <!--  用于显示在原色上方的元素的颜色（例如，文本和图标，根据可访问性，通常为白色或半透明的黑色）  -->

        <!--  字符文本 避免被自动转换为大写  -->
        <item name="textAllCaps">false</item>
        <item name="android:includeFontPadding">false</item>
        <item name="android:scrollbars">none</item>
        <item name="android:overScrollMode">never</item>
    </style>
</resources>